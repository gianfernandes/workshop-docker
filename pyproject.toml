[project]
name = "workshop-docker"
version = "0.1.0"
description = ""
authors = [
    {name = "Gianluca Fernandes do Couto",email = "gcouto@sinaf.com.br"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
